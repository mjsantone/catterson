# Assets: Steering

Shot list from CONTENT-SPEC.md. Drop files here with these exact names, run `node build.js`, and they replace their placeholders. All fresh capture.

## Lead
- `01_save-to-workflow.mp4`: a refinement folds into the saved workflow. Muted loop, autoplay-safe.
- `01_save-to-workflow-poster.jpg` (optional): poster frame.

## Supporting
- `00_chat-input.mp4`: start the task in chat. Muted loop.
- `02_reuse-workflow.mp4`: rerun a task and pick the saved workflow. Muted loop.
- `state-01.png`, `state-02.png`, ... : state stills. Any count works; they render in filename order.
